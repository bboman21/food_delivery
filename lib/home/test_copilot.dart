import 'package:flutter/material.dart';

class TestCopilot extends StatelessWidget {
  const TestCopilot({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: AppBar(),
    );
  }
}
